from app import models

def getIncidents(type, latitude, longitude, time):
	# Retrieve a set of police incidents from the DB
	return None

def createPrediction(type, latitude, longitude, time):
	# Create a prediction entry in the prediction database
	return None

def getPredictions(type, latitude, longitude):
	# Check if we've made previous predictions of this type of crime in the area
	return None

def updatePrediction(type, latitude, longitude):
	# Get the previous predictions we've made for crimes in this area and update them
	return None
